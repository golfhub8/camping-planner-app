# The Camping Planner - Environment Variables Example
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# ===== Server Configuration =====
# Port for the server (default: 3000)
PORT=3000

# Node environment (development, production, test)
NODE_ENV=development

# ===== Database Configuration =====
# PostgreSQL connection string
DATABASE_URL=postgresql://username:password@host:5432/database_name

# Alternative: Individual PostgreSQL connection parameters
PGHOST=localhost
PGPORT=5432
PGUSER=your_db_user
PGPASSWORD=your_db_password
PGDATABASE=camping_planner

# ===== Authentication =====
# Session secret for Express session management
SESSION_SECRET=your-random-secret-key-min-32-characters-long

# JWT secret for token signing (if using JWT authentication)
JWT_SECRET=your-jwt-secret-key-min-32-characters-long

# ===== Stripe Payment Processing =====
# Stripe secret key (get from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe webhook secret (get from https://dashboard.stripe.com/webhooks)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe price ID for Pro Membership
STRIPE_PRICE_ID=price_your_stripe_price_id_here

# For testing: separate test mode credentials
TESTING_STRIPE_SECRET_KEY=sk_test_your_test_key_here
TESTING_VITE_STRIPE_PUBLIC_KEY=pk_test_your_test_public_key_here

# Frontend Stripe public key (Vite env var - must start with VITE_)
VITE_STRIPE_PUBLIC_KEY=pk_live_your_stripe_public_key_here

# ===== Email Configuration (SMTP) =====
# SMTP server configuration for sending emails
SMTP_HOST=smtp.your-email-provider.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password-or-app-specific-password

# Email "from" address
SMTP_FROM=noreply@thecampingplanner.app

# ===== External APIs =====
# National Park Service API key (get from https://www.nps.gov/subjects/developer/get-started.htm)
NPS_API_KEY=your_nps_api_key_here

# Mapbox token for geocoding (get from https://account.mapbox.com/access-tokens/)
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here

# ===== Frontend Configuration =====
# API URL for frontend (only needed if API is on different domain)
# VITE_API_URL=http://localhost:3000

# ===== Notes =====
# - Variables prefixed with VITE_ are exposed to the frontend
# - All other variables are only available on the backend
# - In production, use Replit's Secrets management instead of .env files
# - Generate strong random values for SESSION_SECRET using: openssl rand -base64 32
